FROM arigaio/atlas:latest

WORKDIR /app

# Copy migration files and config
COPY atlas.hcl ./
COPY internal/ent/migrate/migrations ./internal/ent/migrate/migrations

# Run migration apply (atlas image already has atlas as entrypoint)
CMD ["migrate", "apply", "--config", "file://atlas.hcl", "--env", "local"]
